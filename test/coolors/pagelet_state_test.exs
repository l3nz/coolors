defmodule Coolors.PageletStateTest do
  # test/coolors/pagelet_state_test.exs
  use ExUnit.Case, async: true
  doctest Coolors.PageletState
  alias Coolors.PageletState

  # to be done
  describe "pagelets" do
    test "x" do
      assert PageletState.x() == true
    end
  end
end
